import React from 'react'
import { HiArrowRight } from "react-icons/hi";

const Card = ({item:{title,des,icon}}) => {
  return (
    <div className="group flex min-h-80 w-full items-center rounded-lg bg-gradient-to-r from-bodyColor to-[#202327] px-8 py-10 shadow-shadowOne transition-colors duration-300 hover:bg-gradient-to-b hover:from-black hover:to-[#1e2024] sm:px-12">
      <div>
        <div className="flex h-full flex-col gap-8 transition-transform duration-300 lgl:translate-y-4 lgl:group-hover:translate-y-0">
          <div className="w-10 h-8 flex flex-col justify-between">
        
            {icon ? (
              <span className="text-5xl text-designColor">{icon}</span>
            ) : (
              <>
                <span className="w-full h-[2px] rounded-lg bg-designColor inline-flex"></span>
                <span className="w-full h-[2px] rounded-lg bg-designColor inline-flex"></span>
                <span className="w-full h-[2px] rounded-lg bg-designColor inline-flex"></span>
                <span className="w-full h-[2px] rounded-lg bg-designColor inline-flex"></span>
              </>
            )}
          </div>
          <div className="flex flex-col gap-6">
            <h2 className="text-xl md:text-2xl font-titleFont font-bold text-gray-300">
              {title}
            </h2>
            <p className="text-sm leading-6 text-gray-400">{des}</p>
            <span className="text-2xl text-designColor">
              <HiArrowRight />
            </span>
          </div>
        </div>
      </div>
    </div>
  );
}

export default Card
