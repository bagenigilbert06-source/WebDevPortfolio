{
  "name": "web-dev-portfolio",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "dev": "pnpm --filter profolio start",
    "build": "pnpm --filter profolio build",
    "test": "pnpm --filter profolio test"
  }
}
